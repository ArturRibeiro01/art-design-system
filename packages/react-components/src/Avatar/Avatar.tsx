import React from 'react';import * as styles from './Avatar.css';type AvatarProps = {  src: string;  alt?: string;  size?: 'sm' | 'md' | 'lg';  rounded?: boolean;};export const Avatar: React.FC<AvatarProps> = ({                                                src,                                                alt = 'Avatar',                                                size = 'md',                                                rounded = true,                                              }) => {  return (    <img      src={src}      alt={alt}      className={`${styles.base} ${styles.size[size]} ${rounded ? styles.rounded : ''}`}    />  );};